import SpaceClient from "./SpaceClient";

export const metadata = {
  title: "Space | Log Hound",
  description: "Explorador 3D de labels, user agents e IPs para trafico automatizado.",
};

export default function SpacePage() {
  return <SpaceClient />;
}
